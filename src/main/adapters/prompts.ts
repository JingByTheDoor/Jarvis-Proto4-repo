// ─── Shared LLM Prompts ──────────────────────────────────────────────
// Centralised prompt strings shared across adapter implementations.

/** System prompt used for conversational (non-tool) replies. */
export const CHAT_SYSTEM_PROMPT =
  `You are JARVIS, a helpful AI operator console assistant. Respond conversationally and concisely to the user's message.`;
